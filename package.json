{
	"scripts": {
		"push": "git add -A && git commit -m \"$msg\" && git push orgin master:$feature",
		"bootstrap": "lerna bootstrap",
		"test": "lerna run test"
	},
	"devDependencies": {
		"lerna": "^2.8.0"
	}
}
